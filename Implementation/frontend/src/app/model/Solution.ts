import {Account} from './Account';
import {Team} from './Team';

export class Solution {
  solutionId: number;
  s_name: string;
  langage: string;
  version: number;
  raking: number;
  submitDate: Date;
}
